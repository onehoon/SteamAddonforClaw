<Window
    x:Class="SteamInputAddonforClaw.Overlay.OverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="AnimationViewport">
        <Grid x:Name="OpaquePanel" Background="#FFF3F3F3" Padding="24,24,24,16">
            <Grid x:Name="AnimatedContent" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid x:Name="TabStrip" Grid.Row="0" ColumnSpacing="4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                </Grid>

                <ScrollViewer
                    x:Name="BodyScroll"
                    Grid.Row="1"
                    VerticalScrollBarVisibility="Hidden"
                    HorizontalScrollBarVisibility="Disabled"
                    HorizontalContentAlignment="Stretch">
                    <Grid
                        x:Name="TabBody"
                        HorizontalAlignment="Stretch"
                        Width="{Binding ElementName=BodyScroll, Path=ViewportWidth}" />
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
